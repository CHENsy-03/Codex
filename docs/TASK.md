# TASK.md

> 本文件定义当前开发任务。
>
> 所有开发者和 AI 在开始修改代码前，必须先阅读：
>
> 1. 项目根目录的 `AGENTS.md`；
> 2. `docs/SYSTEM_ARCHITECTURE.md`；
> 3. `docs/DEVELOPMENT_RULES.md`；
> 4. 本文件。
>
> 本文件只描述当前任务。
>
> 每次开始新任务时，应覆盖或更新本文件，禁止同时混入多个互不相关的任务。

---

## 1. 任务基本信息

| 项目   | 内容                                                             |
| ---- | -------------------------------------------------------------- |
| 任务名称 | `[填写任务名称]`                                                     |
| 任务编号 | `[例如 TASK-001]`                                                |
| 任务类型 | `[feature / fix / refactor / test / docs / chore]`             |
| 优先级  | `[P0 / P1 / P2 / P3]`                                          |
| 当前状态 | `[draft / ready / in_progress / blocked / review / completed]` |
| 创建日期 | `[YYYY-MM-DD]`                                                 |
| 负责人  | `[开发者或团队名称]`                                                   |
| 执行工具 | `[人工 / Codex / DeepSeek / GPT / 混合]`                           |
| 目标分支 | `[填写 Git 分支名称]`                                                |

---

## 2. 任务背景

说明为什么需要执行本任务。

应包含：

* 当前系统存在的问题；
* 问题出现的场景；
* 对用户或系统造成的影响；
* 为什么现在需要处理；
* 与已有功能的关系。

填写内容：

```text
[填写任务背景]
```

---

## 3. 当前问题

明确描述当前行为。

应尽量包含：

* 输入条件；
* 当前实际结果；
* 正确预期结果；
* 错误日志；
* 复现步骤；
* 涉及模块；
* 已知限制。

### 当前行为

```text
[填写当前系统行为]
```

### 期望行为

```text
[填写期望系统行为]
```

### 复现步骤

1. `[步骤一]`
2. `[步骤二]`
3. `[步骤三]`

### 错误信息

```text
[填写错误日志、异常堆栈或相关输出；没有则写"无"]
```

---

## 4. 任务目标

本任务必须完成以下目标：

* [ ] `[目标一]`
* [ ] `[目标二]`
* [ ] `[目标三]`

目标必须：

* 可以验证；
* 可以测试；
* 范围明确；
* 不使用"优化一下""完善功能"等模糊描述。

推荐写法：

```text
将所有搜索提供者统一到 SearchProvider 接口，
并保证原有 TRS 和 JPAAS 搜索结果格式保持兼容。
```

不推荐写法：

```text
优化搜索功能。
```

---

## 5. 非目标

以下内容不属于本任务范围：

* `[非目标一]`
* `[非目标二]`
* `[非目标三]`

除非开发者明确批准，否则不得处理非目标内容。

示例：

```text
- 不修改数据库表结构；
- 不调整前端界面；
- 不更换 HTTP 客户端；
- 不重构无关采集模块；
- 不升级现有依赖的大版本。
```

---

## 6. 修改范围

### 允许修改的文件或目录

```text
[填写允许修改的文件或目录]
```

示例：

```text
worker/search/
worker/models/search_result.py
server/internal/search/
tests/search/
```

### 允许新增的文件或目录

```text
[填写允许新增的文件或目录]
```

### 禁止修改的文件或目录

```text
[填写禁止修改的文件或目录]
```

示例：

```text
server/internal/auth/
server/migrations/
client/
```

如果实施过程中发现必须修改范围外文件，应停止相关修改，并先说明原因。

---

## 7. 相关代码

开始修改前，应阅读以下内容：

```text
[填写相关文件、接口、数据模型和测试文件]
```

示例：

```text
worker/search/base.py
worker/search/trs.py
worker/search/jpaas.py
worker/models/search_result.py
server/internal/api/search_handler.go
tests/search/test_trs.py
```

同时需要检查：

* 直接调用方；
* 上游输入；
* 下游使用方；
* 公共数据模型；
* 配置项；
* 现有测试；
* 文档说明。

---

## 8. 当前架构约束

本任务必须遵守以下约束：

* Python 负责网页搜索、下载、解析、清洗、评分和内容处理；
* Go 负责 API、调度、任务状态、并发和事务数据；
* 不允许 Python 与 Go 重复实现相同能力；
* 不允许解析器直接访问数据库；
* 不允许业务模块自行创建独立 HTTP 客户端；
* 不允许插件绕过统一配置、日志和存储接口；
* 不允许为了当前任务重新设计整体架构。

本任务额外约束：

* `[填写约束一]`
* `[填写约束二]`
* `[填写约束三]`

---

## 9. 接口与兼容性

### 是否涉及公共接口

```text
[是 / 否]
```

### 涉及的接口

```text
[填写 HTTP API、内部接口、函数签名或通信协议]
```

### 兼容性要求

* [ ] 保持现有 HTTP 路径兼容；
* [ ] 保持现有 JSON 字段兼容；
* [ ] 保持 Python 与 Go 通信协议兼容；
* [ ] 保持数据库数据兼容；
* [ ] 保持配置文件兼容；
* [ ] 保持命令行参数兼容；
* [ ] 不适用。

新增字段时应优先设置为可选字段，并提供合理默认值。

如果必须进行不兼容修改，应明确记录：

```text
[填写不兼容内容、影响范围和迁移方案]
```

---

## 10. 数据模型

### 是否涉及数据模型变更

```text
[是 / 否]
```

### 当前数据结构

```json
{
  "placeholder": "填写当前结构"
}
```

### 目标数据结构

```json
{
  "placeholder": "填写目标结构"
}
```

### 字段说明

| 字段      | 类型     | 必填      | 默认值     | 说明     |
| ------- | ------ | ------- | ------- | ------ |
| `[字段名]` | `[类型]` | `[是/否]` | `[默认值]` | `[说明]` |

未涉及数据模型时，填写"无"。

---

## 11. 数据库影响

### 是否涉及数据库

```text
[是 / 否]
```

### 涉及内容

* [ ] 新增表；
* [ ] 新增字段；
* [ ] 修改字段；
* [ ] 新增索引；
* [ ] 修改查询；
* [ ] 数据迁移；
* [ ] 仅修改 Repository 或 Storage；
* [ ] 不适用。

### 数据库要求

```text
[填写数据库修改内容；不涉及则写"无"]
```

数据库变更必须：

* 使用迁移文件；
* 保持旧数据可用；
* 提供回滚或兼容方案；
* 不直接修改生产数据；
* 不删除现有字段，除非任务明确批准。

---

## 12. 配置影响

### 是否新增或修改配置

```text
[是 / 否]
```

### 配置项

| 配置路径     | 环境变量     | 类型     | 默认值     | 是否敏感    | 说明     |
| -------- | -------- | ------ | ------- | ------- | ------ |
| `[配置路径]` | `[环境变量]` | `[类型]` | `[默认值]` | `[是/否]` | `[说明]` |

配置变更必须同步更新：

* 示例配置；
* 环境变量说明；
* 配置校验；
* 相关文档；
* 测试。

敏感配置不得提供真实值。

---

## 13. 实施步骤

严格按照以下顺序实施。

### 阶段 1：分析

* [ ] 阅读任务相关文档；
* [ ] 阅读相关代码；
* [ ] 确认上下游调用关系；
* [ ] 确认数据流；
* [ ] 确认现有测试；
* [ ] 列出准备修改的文件；
* [ ] 标记潜在兼容性风险。

### 阶段 2：实现

* [ ] `[实现步骤一]`
* [ ] `[实现步骤二]`
* [ ] `[实现步骤三]`
* [ ] `[实现步骤四]`

### 阶段 3：测试

* [ ] 增加或更新单元测试；
* [ ] 运行相关模块测试；
* [ ] 运行 Python 测试；
* [ ] 运行 Go 测试；
* [ ] 运行契约测试；
* [ ] 运行必要的集成测试；
* [ ] 验证错误场景；
* [ ] 验证兼容性。

### 阶段 4：审查

* [ ] 检查 `git status`；
* [ ] 检查 `git diff --stat`；
* [ ] 检查完整 `git diff`；
* [ ] 确认没有无关修改；
* [ ] 确认没有敏感信息；
* [ ] 确认文档已经同步；
* [ ] 输出剩余风险。

---

## 14. 推荐实现方案

描述本任务计划采用的实现方式。

```text
[填写推荐实现方案]
```

内容应包括：

* 修改哪些模块；
* 各模块承担什么职责；
* 数据如何流转；
* 是否新增接口；
* 是否新增插件；
* 如何保持兼容；
* 如何处理失败；
* 如何测试。

本节是执行指导，不允许 AI 擅自替换为另一套架构。

发现更优方案时，可以提出建议，但不得未经确认直接改变方案。

---

## 15. 禁止行为

执行本任务时禁止：

* 扩大任务范围；
* 修改无关模块；
* 顺手重构整个项目；
* 删除现有功能；
* 删除失败测试；
* 跳过错误处理；
* 使用空结果掩盖异常；
* 硬编码配置；
* 引入未经批准的大型依赖；
* 修改生产数据；
* 自动执行 `git push`；
* 自动执行破坏性 Git 命令；
* 自动提交代码，除非任务明确授权；
* 声称未运行的测试已经通过。

本任务额外禁止：

* `[禁止项一]`
* `[禁止项二]`
* `[禁止项三]`

---

## 16. 测试要求

### Python 测试

根据项目现有配置执行：

```bash
pytest
```

如项目已经启用对应工具，再执行：

```bash
ruff check .
mypy .
```

### Go 测试

```bash
go test ./...
go vet ./...
```

涉及并发修改时，建议执行：

```bash
go test -race ./...
```

### 本任务专项测试

```text
[填写本任务必须执行的专项测试命令]
```

### 手动测试步骤

1. `[步骤一]`
2. `[步骤二]`
3. `[步骤三]`

不得请求真实生产服务进行破坏性测试。

---

## 17. 测试用例

至少覆盖以下类型：

### 正常场景

* [ ] `[正常场景一]`
* [ ] `[正常场景二]`

### 边界场景

* [ ] `[边界场景一]`
* [ ] `[边界场景二]`

### 异常场景

* [ ] `[异常场景一]`
* [ ] `[异常场景二]`

### 兼容性场景

* [ ] `[兼容场景一]`
* [ ] `[兼容场景二]`

### 幂等与重复执行

* [ ] 重复请求不会产生错误状态；
* [ ] 重复采集不会产生不受控重复数据；
* [ ] 重复执行任务时结果符合预期；
* [ ] 不适用。

---

## 18. 验收标准

任务必须同时满足以下条件：

* [ ] 所有任务目标已经实现；
* [ ] 所有非目标均未被擅自处理；
* [ ] 修改范围符合本文件约束；
* [ ] 未破坏 Python 与 Go 的职责边界；
* [ ] 未重复实现已有能力；
* [ ] 公共接口保持兼容，或变更已明确记录；
* [ ] 错误可以定位到具体阶段；
* [ ] 日志包含必要上下文；
* [ ] 新增行为具有测试覆盖；
* [ ] 相关测试实际通过；
* [ ] 没有通过删除或跳过测试制造通过结果；
* [ ] 数据库变更具有迁移方案；
* [ ] 配置变更已同步文档；
* [ ] `git diff` 中没有无关修改；
* [ ] 没有敏感信息进入仓库；
* [ ] 未执行未经授权的提交或推送；
* [ ] 剩余风险已经明确说明。

### 本任务专项验收标准

* [ ] `[专项标准一]`
* [ ] `[专项标准二]`
* [ ] `[专项标准三]`

---

## 19. 回滚方案

说明修改出现问题时如何恢复。

```text
[填写回滚步骤]
```

应包含：

* 需要恢复的文件；
* 是否需要回滚数据库迁移；
* 是否需要恢复配置；
* 是否存在新旧数据兼容问题；
* 是否需要清理临时数据；
* 是否需要停止相关 Worker。

如果本任务不涉及运行行为变更，可以填写：

```text
通过恢复本任务涉及的代码和配置文件即可回滚，无数据库数据变更。
```

---

## 20. 风险清单

| 风险      | 影响        | 发生概率      | 应对方式     |
| ------- | --------- | --------- | -------- |
| `[风险一]` | `[高/中/低]` | `[高/中/低]` | `[处理方式]` |
| `[风险二]` | `[高/中/低]` | `[高/中/低]` | `[处理方式]` |

重点检查：

* 接口兼容；
* 数据丢失；
* 重复数据；
* 并发竞争；
* 资源泄漏；
* 外部服务变化；
* 页面结构变化；
* 配置缺失；
* Python 与 Go 协议不一致；
* 测试覆盖不足。

---

## 21. 阻塞问题

当前阻塞项：

```text
[没有则填写"无"]
```

当任务被阻塞时，应记录：

* 阻塞原因；
* 需要谁确认；
* 需要什么信息；
* 已完成到哪一步；
* 哪些修改尚未开始；
* 是否存在可独立继续的工作。

不得通过猜测关键业务规则绕过阻塞问题。

---

## 22. 执行记录

### 修改文件

```text
[任务完成后填写实际修改文件]
```

### 实际执行命令

```text
[填写实际执行的格式化、检查和测试命令]
```

### 测试结果

```text
[填写实际结果，不得填写未运行的测试]
```

### 未执行测试

```text
[填写未执行的测试及原因；全部执行则写"无"]
```

### 兼容性结果

```text
[说明 API、配置、数据库、通信协议和部署是否受影响]
```

### 剩余风险

```text
[填写任务完成后仍然存在的风险]
```

---

## 23. AI 完成任务后的输出格式

AI 完成任务后必须按照以下格式输出：

```markdown
## 修改文件

- `路径`：修改内容
- `路径`：修改内容

## 实现结果

- 完成内容一
- 完成内容二

## 测试结果

- `实际执行命令`：通过 / 失败
- `实际执行命令`：通过 / 失败

## 未执行测试

- 无

## 兼容性影响

- API：无影响
- 配置：无影响
- 数据库：无影响
- Python/Go 协议：无影响
- 部署：无影响

## 剩余风险

- 风险内容，或"无"
```

禁止只回复：

```text
已经完成。
```

---

## 24. 任务关闭

任务关闭前确认：

* [ ] 验收标准全部检查；
* [ ] 测试结果已记录；
* [ ] 未执行测试已说明；
* [ ] 文档已同步；
* [ ] 风险已记录；
* [ ] `git diff` 已人工检查；
* [ ] 是否提交由开发者决定。

任务最终状态：

```text
[completed / partially_completed / blocked / cancelled]
```

任务关闭说明：

```text
[填写最终结果]
```

---

End.


---

## AI 执行规则

执行模型（Codex、DeepSeek）：

- 严格按照 GPT 输出方案执行
- 不得自行改变方案
- 出现阻塞立即停止
- 输出阻塞原因
- 等待新的 GPT 修改方案
