# AGENTS.md

> 本文件适用于仓库根目录及其下所有 `workspace/<project>` 子项目。
>
> 项目级 `AGENTS.md` 可以补充更具体的规则，但不得违反本文件中的安全、Git、最小修改、文档同步和真实报告要求。
>
> 本文件定义仓库级最高开发原则。
>
> 所有 AI 与人工开发者都必须遵守。

---

# 1. 适用范围与优先级

本文件适用于整个多项目仓库。

规则优先级：

1. 用户当前明确要求；
2. 根级 `AGENTS.md`；
3. 当前项目 `AGENTS.md`；
4. 根级 `docs/DEVELOPMENT_RULES.md`；
5. 当前项目架构和任务文档。

规则冲突时，应报告冲突，不得自行选择对业务影响更大的方案。

---

# 2. 修改前必读

开始修改前必须：

- 阅读根级 `AGENTS.md`；
- 阅读当前项目 `AGENTS.md`；
- 阅读项目架构、任务和开发规则；
- 检查当前分支、HEAD、暂存区、工作区和未跟踪文件；
- 理解调用关系、数据流和现有测试；
- 明确允许修改和禁止修改的范围。

未阅读相关代码和规则前，不得直接开始修改。

---

# 3. 项目隔离

每个项目使用独立目录：

```text
workspace/<project>/
```

项目应自行维护：

- 项目级 `AGENTS.md`；
- 项目级 `docs/`；
- 源码、测试、配置和部署文件。

根级规则适用于所有项目；项目规则可以更具体，但不得与根级底线规则冲突。

根级文档不记录单个项目的任务进度、测试数量、提交记录或实现细节。

---

# 4. 最小修改原则

每次任务只修改当前需求直接相关的文件。

禁止：

- 顺手优化无关模块；
- 无关重构；
- 修改未涉及模块；
- 修改无关接口；
- 批量移动目录；
- 替换技术框架；
- 升级无关依赖。

修改范围越小越好。

必须扩大范围时，先停止并说明原因，等待开发者确认。

---

# 5. 工作区保护

所有开发工作必须保护用户预存修改。

禁止：

- 覆盖用户已有修改；
- 删除用户文件；
- 恢复用户已删除内容；
- 清理用户未跟踪文件；
- 使用破坏性命令整理工作区。

修改前记录 Git 基线，修改后核对差异。

---

# 6. 授权边界

AI 不得擅自：

- 修改架构；
- 修改数据库设计；
- 删除模块；
- 引入大型依赖；
- 修改公共接口；
- 扩大任务范围。

除非任务文档或开发者明确授权。

涉及公共接口、数据库、配置、部署和安全边界时，必须先同步文档并说明影响。

---

# 7. Git 授权边界

允许只读检查：

- `git status`；
- `git diff`；
- `git log`；
- 其他只读 Git 检查。

以下操作必须由开发者明确授权：

- 暂存；
- 提交；
- 推送；
- 创建或修改 PR；
- 修改历史；
- 合并、变基或切换分支。

禁止在未授权时执行破坏性 Git 命令。

---

# 8. 测试与验证

每次修改必须运行相关测试，并按项目规定执行静态检查和构建。

测试要求：

- 测试结果必须真实；
- 不得预填或虚构数量；
- 不得删除、跳过或弱化既有测试；
- 安全、失败路径和边界条件应有测试；
- 外部依赖应通过 Mock、fixture 或注入隔离；
- 默认不访问真实外部服务，除非任务明确授权。

不同项目可以使用不同技术栈和测试命令；根级规则不固定任何单一技术栈。

---

# 9. 文档同步

涉及以下变化时必须同步更新文档：

- 公共接口；
- 数据模型；
- 配置项；
- 模块结构；
- 运行方式；
- 任务状态。

禁止代码与文档不一致。

项目实现细节、任务进度和测试结果应记录在项目内部文档。

---

# 10. 安全与秘密信息

禁止向仓库、日志、诊断、结果或错误信息中写入：

- API Key；
- Token；
- 密码；
- Cookie；
- 私钥；
- 证书；
- 数据库生产凭据；
- 个人敏感信息。

敏感配置应使用环境变量或密钥管理服务。

日志和错误必须可定位，但不得泄露秘密值。

---

# 11. 生成文件

生成物、缓存、构建产物、临时文件和大型输出不得随意写入仓库。

输出文件应放入项目规定的输出目录或忽略目录。

未经授权不得提交生成文件。

---

# 12. 出错即停

出现以下情况应立即停止：

- 任务目标不明确；
- 测试失败且无法确定修复方向；
- 架构或公共接口存在冲突；
- 必须扩大修改范围；
- 必须覆盖用户修改；
- 需要开发者确认关键业务规则；
- 发现无法在本任务范围内消除的安全风险。

停止后必须输出：

- 当前执行步骤；
- 已完成修改；
- 修改文件列表；
- 错误日志；
- 阻塞原因；
- 建议交给开发者或设计者分析的问题。

不得通过猜测绕过阻塞。

---

# 13. 报告真实性

所有报告必须：

- 使用真实命令和真实输出；
- 区分已执行、未执行和无法执行；
- 不预填测试数量；
- 不虚构成功结果；
- 明确说明剩余风险；
- 不泄露秘密信息。

禁止使用“全部正常”或“已经完成”代替真实证据。

---

# 14. 项目内部规则

每个项目必须维护自己的 `AGENTS.md` 或 `docs/`，用于描述该项目专属的架构、目录、命令和验收要求。

根级规则与项目内部规则的关系：

- 根级规则默认适用于所有项目；
- 项目规则只能更具体，不能放宽安全、秘密信息、Git 授权、测试真实性和文档同步要求；
- 规则冲突时，先报告冲突，不得自行选择对业务影响更大的方案。

根级文档只维护多项目通用规范，不记录任何单个项目的任务进度、测试数量、提交记录或实现细节。

---

# 15. 完成标准

任务完成前必须确认：

- 当前需求已实现；
- 未扩大修改范围；
- 代码符合项目架构；
- 相关测试已执行并记录真实结果；
- 未执行测试已说明；
- 文档已同步；
- `git diff` 已检查；
- 没有敏感信息；
- 剩余风险已说明；
- 暂存、提交和发布由开发者决定。

---

End.
