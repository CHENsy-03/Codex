# AGENTS.md

> 本文件定义本项目的最高开发原则。
>
> 所有 AI（GPT、Codex、DeepSeek 等）以及开发者都必须遵守。
>
> 当本文件与其它文档发生冲突时，以本文件为准。

---

# 1. 项目使命

本项目目标不是完成某一个网站采集。

本项目最终目标是建设：

**通用、稳定、可维护、可扩展的信息采集平台。**

所有设计必须服务于长期演进。

不得为了短期需求破坏整体架构。

---

# 2. 核心设计原则

所有修改必须满足：

✓ 可维护

✓ 可测试

✓ 可扩展

✓ 可阅读

✓ 可复用

任何修改，都必须比修改前更容易维护。

---

# 3. 最小修改原则

每次任务：

仅修改当前任务需要修改的内容。

禁止：

- 顺手优化
- 无关重构
- 修改未涉及模块
- 修改无关接口

修改范围越小越好。

---

# 4. 先理解，再修改

开始修改之前必须：

- 阅读当前文件
- 阅读调用关系
- 阅读相关接口
- 理解数据流

禁止：

未阅读代码直接修改。

---

# 5. 模块职责必须清晰

Python：

负责：

- 页面获取
- HTML解析
- 文本清洗
- 去重
- 数据评分
- AI分析
- 采集策略

Go：

负责：

- API
- 调度
- 配置
- 并发
- 权限
- 数据持久化
- 服务管理

禁止职责重叠。

---

# 6. 一个功能只能有一个实现

禁止：

Python实现一份。

Go再实现一份。

所有公共能力必须唯一实现。

其它模块调用即可。

---

# 7. 数据流必须单向

推荐流程：

采集

↓

解析

↓

清洗

↓

评分

↓

存储

↓

分析

禁止循环依赖。

禁止跨层调用。

---

# 8. 配置统一管理

所有配置必须来自：

配置文件

或

环境变量。

禁止：

硬编码：

- IP
- 端口
- Cookie
- Token
- Header
- URL
- 路径

---

# 9. 禁止 Magic Number

禁止：

```
timeout = 37

retry = 5

sleep = 2.7
```

必须：

```
timeout = config.http.timeout
```

所有业务参数必须可配置。

---

# 10. 错误必须可定位

禁止：

except:

pass

禁止：

忽略错误。

必须：

返回：

- 错误原因
- 模块
- URL（如果存在）
- Trace

---

# 11. 日志必须可检索

禁止：

print()

统一日志系统。

日志至少包含：

- 时间
- 等级
- 模块
- 内容

错误日志必须包含异常信息。

---

# 12. 数据库存储原则

事务数据：

使用事务数据库。

分析数据：

使用分析数据库。

原始HTML、PDF、图片：

保存文件。

数据库仅保存：

路径

Hash

元数据。

禁止：

把大量HTML直接写入数据库。

---

# 13. 禁止重复代码

相同逻辑：

超过两次出现。

必须：

抽取公共函数。

禁止复制粘贴。

---

# 14. 函数职责

函数应该：

完成一件事情。

推荐：

20~80行。

超过150行：

应该考虑拆分。

---

# 15. 类职责

一个类：

负责一种职责。

禁止：

超级工具类。

禁止：

God Object。

---

# 16. 接口兼容原则

修改接口：

优先保持兼容。

如果必须修改：

同步更新：

- 文档
- 测试
- 调用方

禁止：

偷偷修改JSON字段。

---

# 17. 数据库修改原则

禁止：

直接修改线上数据。

禁止：

删除字段。

必须：

提供迁移方案。

保持兼容。

---

# 18. AI 修改流程

任何AI必须：

① 阅读需求

↓

② 阅读相关代码

↓

③ 列出准备修改的文件

↓

④ 说明原因

↓

⑤ 修改代码

↓

⑥ 运行测试

↓

⑦ 输出总结

禁止直接开始修改。

---

# 19. 测试原则

每次修改必须：

运行对应测试。

Python：

pytest

Go：

go test

禁止：

删除测试。

禁止：

跳过失败测试。

---

# 20. Git原则

允许：

git status

git diff

git log

禁止：

git push

git reset --hard

git clean -fd

删除历史

强制覆盖

所有提交必须由开发者确认。

---

# 21. 安全原则

禁止：

提交：

API Key

密码

Cookie

Token

证书

私钥

必须：

使用：

.env

环境变量。

---

# 22. AI 不允许擅自决定

AI不得：

- 修改架构
- 修改数据库设计
- 删除模块
- 引入大型依赖
- 修改公共接口

除非：

TASK.md

明确要求。

---

# 23. 文档同步原则

涉及：

接口

数据库

配置

模块

行为变化

必须同步更新文档。

禁止：

代码和文档不一致。

---

# 24. 最终目标

所有修改必须回答四个问题：

① 是否更简单？

② 是否更稳定？

③ 是否更容易维护？

④ 是否更容易扩展？

如果答案是否定的，

请重新设计。

---

End.
